// program to find longest common prefix
s= ["flower","flag","flow"]
prefix=s[0]
for i in s[1:]:
    while  not i.startswith(prefix):
        prefix=prefix[:-1]
        if not prefix:
            print(" ")
print(prefix)
        
